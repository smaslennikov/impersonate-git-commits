# Faking Gabriel Fok

This repository is just a POC of impersonating someone else on GitHub.

## Process

1. Edit `.git/config` within the repository as in [the checked-in example](gitconfig).
1. `git commit` away!
